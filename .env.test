# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# Force le mode web pour que les pages d'erreur personnalisees soient
# rendues dans les tests (sinon le CliErrorRenderer prend la main).
APP_RUNTIME_MODE='web=1'
